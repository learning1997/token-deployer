{
  "name": "opnet-token-launcher",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:contract && tsc && vite build",
    "build:contract": "npx asc contracts/MintableToken.ts --target release",
    "preview": "vite preview"
  },
  "dependencies": {
    "@btc-vision/as-bignum": "latest",
    "@btc-vision/bitcoin": "rc",
    "@btc-vision/btc-runtime": "rc",
    "@btc-vision/transaction": "rc",
    "opnet": "rc"
  },
  "devDependencies": {
    "@assemblyscript/loader": "latest",
    "@btc-vision/assemblyscript": "^0.29.2",
    "@btc-vision/opnet-transform": "1.1.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vite-plugin-node-polyfills": "^0.21.0"
  }
}
